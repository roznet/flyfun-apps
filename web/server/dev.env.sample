# Euro AIP Web Server Configuration
ENVIRONMENT=development
AIRPORTS_DB=./airports.db
RULES_JSON=./rules.json
LOG_LEVEL=INFO

# GA Friendliness Configuration (optional)
# Path to ga_meta.sqlite database (comment out or leave empty to disable)
#GA_META_DB=../tmp/ga_meta.sqlite

# GA database read-only mode (default: true for production safety)
# Set to false only for build tools that need to write to the database
GA_META_READONLY=true

